# Alteryx geom data parsing tool

This macro parses the selected geom field and outputs a new GeoJSON field.

An additional option enables the conversion of the GeoJSON field into an Alteryx spatial object.
